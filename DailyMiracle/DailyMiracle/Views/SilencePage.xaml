<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="DailyMiracle.Views.SilencePage"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:DailyMiracle.ViewModels"
             Title="{Binding Title}">

    <ContentPage.BindingContext>
        <viewModels:SilenceViewModel />
    </ContentPage.BindingContext>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Image Grid.Row="0" Source="silence.png" />
        <Label Grid.Row="1"
               FontSize="50"
               HorizontalTextAlignment="Center"
               Text="{Binding Time}" />
        <Label Grid.Row="2"
               FontSize="Large"
               HorizontalTextAlignment="Center"
               Text="{Binding Description}"
               VerticalTextAlignment="Center" />
        <Button Grid.Row="3"
                Command="{Binding StartTimerCommand}"
                Text="Начать" />
    </Grid>
</ContentPage>
